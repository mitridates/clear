{#
Organisation. Edit
@controller BackendOrganisationController:editAction
@route  admin/organisation/edit/{id}
@path admin_organisation_edit
@var FormView form
@var Area entity
#}
{% do addCrumb('organisation.index.title'|trans({},'cavepages'), path('admin_organisation_index')) %}
{% do initPage(
    'organisation',
    'edit',
    'organisation.edit.title'|trans({},'cavepages'))
        .set('heading', 'organisation.edit.heading'|trans({},'cavepages'))
%}

{% extends '@admin/base.html.twig' %}
{% block subheading %}
{% include '@admin/organisation/subheading_buttons.html.twig'%}
{% endblock %}

{% block content %}
    {% include '@admin/organisation/organisation_form.html.twig' %}
{% endblock %}
{% block footer_javascripts %}

    <script>
        (new Repopulate('#{{ form.country.vars.id }}')).populate();
        (new Repopulate('#{{ form.countryaddress.vars.id }}')).populate();
        JXhrSubmit("#{{form.vars.attr.id }}");
        JSuggest(document.querySelector('.js-organisation'))

        </script>
{% endblock %}
