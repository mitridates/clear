{#
Href as button
#}
{%- set attr= (attr ?? {})|merge({
    'class':  'btn'~(' '~attr.class ?? '')~(' '~class??''),
    'href': href ?? attr.href|default('javascript:void(0);')
}) -%}
<a {% for key, value in attr %} {{ key }}="{{ value|raw }}" {% endfor %}> {{ text|default('href text')}}</a>