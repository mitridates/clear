{#
@see ./bttn_.html.twig
#}
{%- set attr= (attr ?? {})|merge({
    'class':  'btn btn-primary btn-outline-default'~(' '~attr.class ?? '')~(' '~class??''),
    'href': href ?? attr.href|default('javascript:void(0);')
}) -%}
<a {% for key, value in attr %} {{ key }}="{{ value|raw }}" {% endfor %}><i class="fa fa-edit"></i> {{ text|default('btn.edit'|trans({},'cavemessages')) -}}</a>