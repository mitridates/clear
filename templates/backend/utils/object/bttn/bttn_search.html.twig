{#
@see ./bttn_.html.twig
#}
{%- set attr= (attr ?? {})|merge({
    'type':   attr.type ?? type|default('submit'),
    'id': attr.id ?? id|default(''),
    'title': attr.title ?? title|default(''),
    'class':  'btn btn-default btn-outline-success js-search'~(' '~attr.class ?? '')~(' '~class??''),
    'onClick':  attr.onClick ?? onClick|default(''),
}) -%}
<button {% for key, value in attr|filter(value => value != '') %} {{ key }}="{{ value|raw }}" {% endfor %}><i class="fa fa-search"></i> {{ text|default('btn.search'|trans({},'cavemessages'))|raw -}}</button>